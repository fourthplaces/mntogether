{
  "name": "mndigitalaid-infra",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -A run build",
    "clean": "yarn workspaces foreach -A run clean"
  },
  "packageManager": "yarn@4.1.0"
}
